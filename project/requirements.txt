# Python dependencies for the SARC sarcasm-detection project.
# Install with:  pip install -r requirements.txt
# Tested with Python 3.10+.

# --- Deep learning / transformers ---
torch>=2.1            # PyTorch; provides CUDA (NVIDIA) and MPS (Apple Silicon) backends
transformers>=4.46    # BERT/RoBERTa + Trainer; >=4.46 for `processing_class` (tested on 5.x)
datasets>=2.18        # Hugging Face dataset containers
accelerate>=0.27      # backs the Trainer (device placement, mixed precision)

# --- Classical ML / baseline ---
scikit-learn>=1.3     # TF-IDF, LogisticRegression, LinearSVC, metrics, splitting

# --- Data handling ---
pandas>=2.0
numpy>=1.24
scipy>=1.10           # binomial test used by McNemar

# --- Plotting ---
matplotlib>=3.7       # confusion-matrix figures
